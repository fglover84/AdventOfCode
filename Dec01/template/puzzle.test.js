const {} = require("./puzzle.js");
const { input } = require("./input.js");

const testData = [];

describe("given the input ", () => {
  test("should check ...", () => {
    expect(findSumOfPartNumbers(input)).toEqual(null);
  });
});
